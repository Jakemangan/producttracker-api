export interface UserData {
    id: string;
    name: string;
    userRole: number;
    email: string;
}

export interface UserDataDbo {
    id: string;
    name: string;
    user_role: number;
    email: string;
}